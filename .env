# Database
DATABASE_URL=file:./db/custom.db

# Admin secret for destructive operations (seed endpoint)
# Set this in production to protect the /api/seed endpoint
# ADMIN_SECRET=change-me-to-a-strong-secret

# Node environment
NODE_ENV=development
